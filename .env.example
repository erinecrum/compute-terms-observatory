# Copy this file to `.env` and fill in your value. `.env` is gitignored — never commit it.
#
# The API key is only needed for the extraction layer (build step 3 onward).
# The fetch / snapshot / diff layers run with no key at all, so the archival
# corpus can be built and updated without any secret present.
ANTHROPIC_API_KEY=sk-ant-...
