# Compute Contract Terms Observatory — runtime dependencies (kept deliberately minimal,
# matching the radar project's low-dependency posture).
requests>=2.31        # HTTP fetching of public terms/SLA/AUP pages
beautifulsoup4>=4.12  # HTML parsing / content extraction for page snapshots
PyYAML>=6.0           # source registry + human-verified overrides (non-confidential)
python-dotenv>=1.0    # load ANTHROPIC_API_KEY from a local .env (never committed)
anthropic>=0.40       # Claude API client for the structured extraction layer
pypdf>=4.0            # extract text from PDF-hosted documents (e.g. Microsoft MCA)
openpyxl>=3.1         # write the downloadable Excel (.xlsx) comparison export
python-docx>=1.1      # extract text from DOCX-hosted documents (e.g. Azure consolidated SLA)
